# Parse.from("input")
# |> IO.inspect(label: "monkeys")
# |> Puzzle1.solve()
# |> IO.inspect(label: "value")

Parse.from("input")
|> IO.inspect(label: "monkeys")
|> Puzzle2.solve()
|> IO.inspect(label: "value")
